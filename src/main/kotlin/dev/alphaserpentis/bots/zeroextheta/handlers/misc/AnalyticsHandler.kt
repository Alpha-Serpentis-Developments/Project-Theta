package dev.alphaserpentis.bots.zeroextheta.handlers.misc

class AnalyticsHandler {

    /**
     * Returns the raw analytics data collected for the specified ID (guild or user)
     */
    fun infoOn(id: Long) {
        TODO()
    }
}
